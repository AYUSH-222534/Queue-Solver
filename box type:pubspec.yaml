name: queue_solve
description: NCERT Doubt Solver for Class 6-12
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  google_sign_in: ^6.2.1
  image_picker: ^1.1.2
  google_mlkit_text_recognition: ^0.13.0
  http: ^1.2.2
  flutter_markdown: ^0.7.3
  speech_to_text: ^7.0.0
  permission_handler: ^11.3.1
  pinput: ^5.0.0
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/google_logo.png

Do I need to upload this file in yaml?